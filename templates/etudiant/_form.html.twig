{{ form_start(form,{'attr': {'novalidate': 'novalidate'}}) }}

<table align="center" class="table">
  <tbody>
    <tr>
      <th class="intitule"><label for="etudiant_nom" class="required">Nom</label></th>
      <td >{{ form_widget(form.nom) }}</td>
    </tr>
    <tr>
      <th class="intitule"><label for="etudiant_prenom" class="required">Prénom</label></th>
      <td >{{ form_widget(form.prenom) }}</td>
    </tr>
    <tr>
      <th class="intitule"><label for="etudiant_mail" class="required">Mail</label></th>
      <td >{{ form_widget(form.mail) }}</td>
    </tr>
    <tr>
      <th class="intitule"><label for="etudiant_estDemissionaire" class="required">Démissionaire</label></th>
      <td >{{form_widget(form.estDemissionaire)}}</td>
    </tr>
  </tbody>
</table>

<div class='bouton'>
  <a href="{{ path('etudiant_index') }}" class="d-none d-sm-inline-block btn btn-sm btn-secondary shadow-sm">Annuler</a>
  <button class="d-none d-sm-inline-block btn btn-sm btn-primary shadow-sm">{{ button_label|default('Enregistrer') }}</button>
</div>

{{ form_end(form) }}
