{% extends 'base.html.twig' %}

{% block tirePage %}Consulter une évaluation{% endblock %}

{% block evaluations %}active{% endblock %}

{% block contenu %}

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ evaluation.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ evaluation.nom }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ evaluation.date }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('evaluation_index') }}">back to list</a>

    <a href="{{ path('evaluation_edit', {'id': evaluation.id}) }}">edit</a>

    {{ include('evaluation/_delete_form.html.twig') }}
{% endblock %}
