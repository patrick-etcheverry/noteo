{{ form_start(form) }}

<table align="center" class="table">
  <tbody>
    <tr>
      <td class="intitule">{{ form_label(form.nom) }}</td>
      <td>{{ form_widget(form.nom) }}</td>
    </tr>
    <tr>
      <td class="intitule">{{ form_label(form.description) }}</td>
      <td>{{ form_widget(form.description) }}</td>
    </tr>
    <tr>
      <td class="intitule">{{ form_label(form.estEvaluable) }}<a href="#"><i class="icon-help-circled"></i></a></td>
      <td>{{form_widget(form.estEvaluable)}}</td>
    </tr>
    <tr>
      <td class="intitule">{{ form_label(form.fichier) }} (<a href="{{asset('template.csv')}}">Télécharger un modèle</a>)</td>
      <td>
        {{form_widget(form.fichier)}}
      </td>
    </tr>
    <tr>
      <td class="intitule">{{ form_label(form.enseignant) }}</td>
      <td>{{ form_widget(form.enseignant) }}</td>
    </tr>
  </tbody>
</table>

<div class='bouton'>
  <button class="d-none d-sm-inline-block btn btn-sm btn-primary shadow-sm">{{ button_label|default('Créer le groupe') }}</button>

{{ form_end(form) }}
