{% extends 'base.html.twig' %}

{% block titrePage %}Choisir les statistiques que vous désirez consulter{% endblock %}

{% block statistiques %}active{% endblock %}

{% block stylesheets %}
    <style>
        .card:hover{
            -webkit-box-shadow: -1px 9px 40px -12px rgba(0,0,0,0.75);
            -moz-box-shadow: -1px 9px 40px -12px rgba(0,0,0,0.75);
            box-shadow: -1px 9px 40px -12px rgba(0, 0, 0, 0.75);
        }

        /* Style the Image Used to Trigger the Modal */
        .imageAAgrandir {
            border-radius: 5px;
            cursor: pointer;
            transition: 0.3s;
        }

        .imageAAgrandir:hover {opacity: 0.7;}

        /* The Modal (background) */
        .modaleImage {
            display: none; /* Hidden by default */
            position: fixed; /* Stay in place */
            z-index: 1; /* Sit on top */
            padding-top: 100px; /* Location of the box */
            left: 0;
            top: 0;
            width: 100%; /* Full width */
            height: 100%; /* Full height */
            overflow: auto; /* Enable scroll if needed */
            background-color: rgb(0,0,0); /* Fallback color */
            background-color: rgba(0,0,0,0.9); /* Black w/ opacity */
        }

        /* Modal Content (Image) */
        .modaleImage-content {
            margin: auto;
            display: block;
            width: 100%;
        }

        /* Add Animation - Zoom in the Modal */
        .modaleImage-content, #caption {
            animation-name: zoom;
            animation-duration: 0.6s;
        }

        @keyframes zoom {
            from {transform:scale(0)}
            to {transform:scale(1)}
        }

        /* The Close Button */
        .close-modaleImage {
            position: absolute;
            top: 15px;
            right: 35px;
            color: #f1f1f1;
            font-size: 40px;
            font-weight: bold;
            transition: 0.3s;
        }

        .close-modaleImage:hover,
        .close-modaleImage:focus {
            color: #bbb;
            text-decoration: none;
            cursor: pointer;
        }

        /* 100% Image Width on Smaller Screens */
        @media only screen and (max-width: 700px){
            .modal-content {
                width: 100%;
            }
        }
    </style>
{% endblock %}

{% block contenu %}
{% if statsClassiquesDispo or statsClassiquesParPartiesDispo or statsPlusieursEvalsGroupeDispo or statsPlusieursEvalsStatutDispo or statsComparaisonDispo %}

        <!-- STATS CLASSIQUES -->
        <div class="card-deck">
            <div class="card">
                <img class="card-img-top imageAAgrandir" {% if statsClassiquesDispo %}src="{{ asset('img/statistiques/classiques/histo.png') }}"{% else %}src="{{ asset('img/statistiques/classiques/histo_black.png') }}"{% endif %} alt="Card image cap">
                <div class="card-body">
                    <hr>
                    <h5 class="card-title">Statistiques classiques sur une évaluation (répartition des notes)</h5>
                    <p class="card-text">
                        Génération de statistiques triviales : Répartition des notes, moyenne, médiane, écart-type, note minimale et maximale. <br>
                        Ces statistiques sont générées pour des évaluations avec une seule note par étudiant.
                    </p>
                    {% if not statsClassiquesDispo %}
                        <span style="font-size:15px; color: #0275d8"><i class="icon-attention-circled"></i>Aucune évaluation sans parties</span> <br>
                    {% endif %}
                    <button {% if not statsClassiquesDispo %}disabled{% endif %} onclick="location.href='{{ path('statistiques_choix_evaluation', {'typeStat' : 'classique', 'typeGraph' : 'histogramme'}) }}'" class="btn {% if statsClassiquesDispo %}btn-primary{% else %}btn-secondary{% endif %}">Consulter ce type de statistiques</button>
                </div>
            </div>
            <div class="card">
                <img class="card-img-top imageAAgrandir" id="myImg" {% if statsClassiquesDispo %}src="{{ asset('img/statistiques/classiques/bam.png') }}"{% else %}src="{{ asset('img/statistiques/classiques/bam_black.png') }}"{% endif %}  alt="Card image cap">
                <div class="card-body">
                    <hr>
                    <h5 class="card-title">Statistiques classiques sur une évaluation (boîte à moustache)</h5>
                    <p class="card-text">
                        Génération de statistiques triviales : Répartition des notes, moyenne, médiane, écart-type, note minimale et maximale. <br>
                        Ces statistiques sont générées pour des évaluations avec une seule note par étudiant.
                    </p>
                    {% if not statsClassiquesDispo %}
                        <span style="font-size:15px; color: #0275d8"><i class="icon-attention-circled"></i>Aucune évaluation sans parties</span> <br>
                    {% endif %}
                    <button {% if not statsClassiquesDispo %}disabled{% endif %} onclick="location.href='{{ path('statistiques_choix_evaluation', {'typeStat' : 'classique', 'typeGraph' : 'boite-moustache'}) }}'" class="btn {% if statsClassiquesDispo %}btn-primary{% else %}btn-secondary{% endif %}">Consulter ce type de statistiques</button>
                </div>
            </div>
            <div class="card">
                <img class="card-img-top imageAAgrandir" {% if statsClassiquesDispo %}src="{{ asset('img/statistiques/classiques/moy.png') }}"{% else %}src="{{ asset('img/statistiques/classiques/moy_black.png') }}"{% endif %} alt="Card image cap">
                <div class="card-body">
                    <hr>
                    <h5 class="card-title">Statistiques classiques sur une évaluation (moyenne et écart-type)</h5>
                    <p class="card-text">
                        Génération de statistiques triviales : Répartition des notes, moyenne, médiane, écart-type, note minimale et maximale. <br>
                        Ces statistiques sont générées pour des évaluations avec une seule note par étudiant.
                    </p>
                    {% if not statsClassiquesDispo %}
                        <span style="font-size:15px; color: #0275d8"><i class="icon-attention-circled"></i>Aucune évaluation sans parties</span> <br>
                    {% endif %}
                    <button {% if not statsClassiquesDispo %}disabled{% endif %} onclick="location.href='{{ path('statistiques_choix_evaluation', {'typeStat' : 'classique', 'typeGraph' : 'moyenne'}) }}'" class="btn {% if statsClassiquesDispo %}btn-primary{% else %}btn-secondary{% endif %}">Consulter ce type de statistiques</button>
                </div>
            </div>
        </div>
        <br>
        <!-- STATS CLASSIQUES AVEC PARTIES -->
        <div class="card-deck">
            <div class="card">
                <img class="card-img-top imageAAgrandir" {% if statsClassiquesParPartiesDispo %}src="{{ asset('img/statistiques/classiques-avec-parties/histo.png') }}"{% else %}src="{{ asset('img/statistiques/classiques-avec-parties/histo_black.png') }}"{% endif %} alt="Card image cap">
                <div class="card-body">
                    <hr>
                    <h5 class="card-title">Statistiques classiques sur une évaluation avec parties (répartition des notes)</h5>
                    <p class="card-text">
                        Génération de statistiques triviales : Répartition des notes, moyenne, médiane, écart-type, note minimale et maximale. <br>
                        Ces statistiques sont générées pour des évaluations avec une seule note par étudiant.
                    </p>
                    {% if not statsClassiquesParPartiesDispo %}
                        <span style="font-size:15px; color: #0275d8"><i class="icon-attention-circled"></i>Aucune évaluation avec parties</span> <br>
                    {% endif %}
                    <button {% if not statsClassiquesParPartiesDispo %}disabled{% endif %} onclick="location.href='{{ path('statistiques_choix_evaluation', {'typeStat' : 'classique-avec-parties', 'typeGraph' : 'histogramme'}) }}'" class="btn {% if statsClassiquesParPartiesDispo %}btn-primary{% else %}btn-secondary{% endif %}">Consulter ce type de statistiques</button>
                </div>
            </div>
            <div class="card">
                <img class="card-img-top imageAAgrandir" id="myImg" {% if statsClassiquesParPartiesDispo %}src="{{ asset('img/statistiques/classiques-avec-parties/bam.png') }}"{% else %}src="{{ asset('img/statistiques/classiques-avec-parties/bam_black.png') }}"{% endif %} alt="Card image cap">
                <div class="card-body">
                    <hr>
                    <h5 class="card-title">Statistiques classiques sur une évaluation avec parties (boîte à moustache)</h5>
                    <p class="card-text">
                        Génération de statistiques triviales : Répartition des notes, moyenne, médiane, écart-type, note minimale et maximale. <br>
                        Ces statistiques sont générées pour des évaluations avec une seule note par étudiant.
                    </p>
                    {% if not statsClassiquesParPartiesDispo %}
                        <span style="font-size:15px; color: #0275d8"><i class="icon-attention-circled"></i>Aucune évaluation avec parties</span> <br>
                    {% endif %}
                    <button {% if not statsClassiquesParPartiesDispo %}disabled{% endif %} onclick="location.href='{{ path('statistiques_choix_evaluation', {'typeStat' : 'classique-avec-parties', 'typeGraph' : 'boite-moustache'}) }}'" class="btn {% if statsClassiquesParPartiesDispo %}btn-primary{% else %}btn-secondary{% endif %}">Consulter ce type de statistiques</button>
                </div>
            </div>
            <div class="card">
                <img class="card-img-top imageAAgrandir" {% if statsClassiquesParPartiesDispo %}src="{{ asset('img/statistiques/classiques-avec-parties/moy.png') }}"{% else %}src="{{ asset('img/statistiques/classiques-avec-parties/moy_black.png') }}"{% endif %} alt="Card image cap">
                <div class="card-body">
                    <hr>
                    <h5 class="card-title">Statistiques classiques sur une évaluation avec parties (moyenne et écart-type)</h5>
                    <p class="card-text">
                        Génération de statistiques triviales : Répartition des notes, moyenne, médiane, écart-type, note minimale et maximale. <br>
                        Ces statistiques sont générées pour des évaluations avec une seule note par étudiant.
                    </p>
                    {% if not statsClassiquesParPartiesDispo %}
                        <span style="font-size:15px; color: #0275d8"><i class="icon-attention-circled"></i>Aucune évaluation avec parties</span> <br>
                    {% endif %}
                    <button {% if not statsClassiquesParPartiesDispo %}disabled{% endif %} onclick="location.href='{{ path('statistiques_choix_evaluation', {'typeStat' : 'classique-avec-parties', 'typeGraph' : 'moyenne'}) }}'" class="btn {% if statsClassiquesParPartiesDispo %}btn-primary{% else %}btn-secondary{% endif %}">Consulter ce type de statistiques</button>
                </div>
            </div>
        </div>
        <br>
        <!--  STATS PLUSIEURS EVALS, GROUPE  -->
        <div class="card-deck">
            <div class="card">
                <img class="card-img-top imageAAgrandir" {% if statsPlusieursEvalsGroupeDispo %}src="{{ asset('img/statistiques/plusieurs-evals-groupe/histo.png') }}"{% else %}src="{{ asset('img/statistiques/plusieurs-evals-groupe/histo_black.png') }}"{% endif %} alt="Card image cap">
                <div class="card-body">
                    <hr>
                    <h5 class="card-title">Statistiques classiques sur plusieurs évaluations par groupe (répartition des notes)</h5>
                    <p class="card-text">Générations de statistiques triviales : répartition des notes, moyenne, médiane, écart-type, note minimale et maximale. <br>
                        Le calcul est effectué sur la note finale de chaque évaluation.</p>
                    {% if not statsPlusieursEvalsGroupeDispo %}
                        <span style="font-size:15px; color: #0275d8"><i class="icon-attention-circled"></i>Aucun groupe ou aucun groupe avec des étudiants ou pas assez d'évaluations</span> <br>
                    {% endif %}
                    <button {% if not statsPlusieursEvalsGroupeDispo %}disabled{% endif %} onclick="location.href='{{ path('stats_choisir_groupe_haut_niveau_evaluable', {'typeGraph': 'histogramme'}) }}'" class="btn {% if statsPlusieursEvalsGroupeDispo %}btn-primary{% else %}btn-secondary{% endif %}">Consulter ce type de statistiques</button>

                </div>
            </div>
            <div class="card">
                <img class="card-img-top imageAAgrandir" id="myImg" {% if statsPlusieursEvalsGroupeDispo %}src="{{ asset('img/statistiques/plusieurs-evals-groupe/bam.png') }}"{% else %}src="{{ asset('img/statistiques/plusieurs-evals-groupe/bam_black.png') }}"{% endif %} alt="Card image cap">
                <div class="card-body">
                    <hr>
                    <h5 class="card-title">Statistiques classiques sur plusieurs évaluations par groupe (boîte à moustache)</h5>
                    <p class="card-text">Générations de statistiques triviales : répartition des notes, moyenne, médiane, écart-type, note minimale et maximale. <br>
                        Le calcul est effectué sur la note finale de chaque évaluation.</p>
                    {% if not statsPlusieursEvalsGroupeDispo %}
                        <span style="font-size:15px; color: #0275d8"><i class="icon-attention-circled"></i>Aucun groupe ou aucun groupe avec des étudiants ou pas assez d'évaluations</span> <br>
                    {% endif %}
                    <button {% if not statsPlusieursEvalsGroupeDispo %}disabled{% endif %} onclick="location.href='{{ path('stats_choisir_groupe_haut_niveau_evaluable', {'typeGraph': 'boite-moustache'}) }}'" class="btn {% if statsPlusieursEvalsGroupeDispo %}btn-primary{% else %}btn-secondary{% endif %}">Consulter ce type de statistiques</button>
                </div>
            </div>
            <div class="card">
                <img class="card-img-top imageAAgrandir" {% if statsPlusieursEvalsGroupeDispo %}src="{{ asset('img/statistiques/plusieurs-evals-groupe/moy.png') }}"{% else %}src="{{ asset('img/statistiques/plusieurs-evals-groupe/moy_black.png') }}"{% endif %} alt="Card image cap">
                <div class="card-body">
                    <hr>
                    <h5 class="card-title">Statistiques classiques sur plusieurs évaluations par groupe (moyenne et écart-type)</h5>
                    <p class="card-text">
                        Générations de statistiques triviales : répartition des notes, moyenne, médiane, écart-type, note minimale et maximale. <br>
                        Le calcul est effectué sur la note finale de chaque évaluation.
                    </p>
                    {% if not statsPlusieursEvalsGroupeDispo %}
                        <span style="font-size:15px; color: #0275d8"><i class="icon-attention-circled"></i>Aucun groupe ou aucun groupe avec des étudiants ou pas assez d'évaluations</span> <br>
                    {% endif %}
                    <button {% if not statsPlusieursEvalsGroupeDispo %}disabled{% endif %} onclick="location.href='{{ path('stats_choisir_groupe_haut_niveau_evaluable', {'typeGraph': 'moyenne'}) }}'" class="btn {% if statsPlusieursEvalsGroupeDispo %}btn-primary{% else %}btn-secondary{% endif %}">Consulter ce type de statistiques</button>
                </div>
            </div>
        </div>
        <br>
        <!-- STATS PLUSIEURS EVALS, STATUT -->
        <div class="card-deck">
            <div class="card">
                <img class="card-img-top imageAAgrandir" {% if statsPlusieursEvalsStatutDispo %}src="{{ asset('img/statistiques/plusieurs-evals-statut/histo.png') }}"{% else %}src="{{ asset('img/statistiques/plusieurs-evals-statut/histo_black.png') }}"{% endif %} alt="Card image cap">
                <div class="card-body">
                    <hr>
                    <h5 class="card-title">Statistiques classiques sur plusieurs évaluations par statut (répartition des notes)</h5>
                    <p class="card-text">
                        Générations de statistiques triviales : répartition des notes, moyenne, médiane, écart-type, note minimale et maximale. <br>
                        Le calcul est effectué sur la note finale de chaque évaluation.
                    </p>
                    {% if not statsPlusieursEvalsStatutDispo %}
                        <span style="font-size:15px; color: #0275d8"><i class="icon-attention-circled"></i>Aucun statut ou aucun statut avec des étudiants ou pas assez d'évaluations</span> <br>
                    {% endif %}
                    <button {% if not statsPlusieursEvalsStatutDispo %}disabled{% endif %} onclick="location.href='{{ path('stats_choisir_statut', {'typeGraph': 'histogramme'}) }}'" class="btn {% if statsPlusieursEvalsStatutDispo %}btn-primary{% else %}btn-secondary{% endif %}">Consulter ce type de statistiques</button>
                    </div>
            </div>
            <div class="card">
                <img class="card-img-top imageAAgrandir" id="myImg" {% if statsPlusieursEvalsStatutDispo %}src="{{ asset('img/statistiques/plusieurs-evals-statut/bam.png') }}"{% else %}src="{{ asset('img/statistiques/plusieurs-evals-statut/bam_black.png') }}"{% endif %} alt="Card image cap">
                <div class="card-body">
                    <hr>
                    <h5 class="card-title">Statistiques classiques sur plusieurs évaluations par statut (boîte à moustache)</h5>
                    <p class="card-text">Générations de statistiques triviales : répartition des notes, moyenne, médiane, écart-type, note minimale et maximale. <br>
                        Le calcul est effectué sur la note finale de chaque évaluation.</p>
                    {% if not statsPlusieursEvalsStatutDispo %}
                        <span style="font-size:15px; color: #0275d8"><i class="icon-attention-circled"></i>Aucun statut ou aucun statut avec des étudiants ou pas assez d'évaluations</span> <br>
                    {% endif %}
                    <button {% if not statsPlusieursEvalsStatutDispo %}disabled{% endif %} onclick="location.href='{{ path('stats_choisir_statut', {'typeGraph': 'boite-moustache'}) }}'" class="btn {% if statsPlusieursEvalsStatutDispo %}btn-primary{% else %}btn-secondary{% endif %}">Consulter ce type de statistiques</button>
                </div>
            </div>
            <div class="card">
                <img class="card-img-top imageAAgrandir" {% if statsPlusieursEvalsStatutDispo %}src="{{ asset('img/statistiques/plusieurs-evals-statut/moy.png') }}"{% else %}src="{{ asset('img/statistiques/plusieurs-evals-statut/moy_black.png') }}"{% endif %} alt="Card image cap">
                <div class="card-body">
                    <hr>
                    <h5 class="card-title">Statistiques classiques sur plusieurs évaluations par statut (moyenne et écart-type)</h5>
                    <p class="card-text">
                        Générations de statistiques triviales : répartition des notes, moyenne, médiane, écart-type, note minimale et maximale. <br>
                        Le calcul est effectué sur la note finale de chaque évaluation.
                    </p>
                    {% if not statsPlusieursEvalsStatutDispo %}
                        <span style="font-size:15px; color: #0275d8"><i class="icon-attention-circled"></i>Aucun statut ou aucun statut avec des étudiants ou pas assez d'évaluations</span> <br>
                    {% endif %}
                    <button {% if not statsPlusieursEvalsStatutDispo %}disabled{% endif %} onclick="location.href='{{ path('stats_choisir_statut', {'typeGraph': 'moyenne'}) }}'" class="btn {% if statsPlusieursEvalsStatutDispo %}btn-primary{% else %}btn-secondary{% endif %}">Consulter ce type de statistiques</button>
                </div>
            </div>
        </div>
        <br>
        <!-- STATS COMPARAISON -->
        <div class="card-deck">
            <div class="card">
                <img class="card-img-top imageAAgrandir" {% if statsComparaisonDispo %}src="{{ asset('img/statistiques/comparaison/moy.png') }}"{% else %}src="{{ asset('img/statistiques/comparaison/moy_black.png') }}"{% endif %} alt="Card image cap">
                <div class="card-body">
                    <hr>
                    <h5 class="card-title">Évolution d'un groupe ou d'un groupe d'étudiants ayant un statut particulier sur plusieurs évaluations</h5>
                    <p class="card-text">
                        Génération de statistiques de comparaison de résultats : Comparaison de la moyenne d'un groupe ou d'un groupe d'étudiants <br>
                        ayant un statut particulier à une évaluation avec la moyenne d'un ensemble d'autres évaluations passées par ces mêmes groupes.
                    </p>
                    {% if not statsComparaisonDispo %}
                        <span style="font-size:15px; color: #0275d8"><i class="icon-attention-circled"></i>Pas assez d'évaluations à comparer</span> <br>
                    {% endif %}
                    <button {% if not statsComparaisonDispo %}disabled{% endif %} onclick="location.href='{{ path('statistiques_choix_evaluation', {'typeStat' : 'comparaison', 'typeGraph' : 'moyenne'}) }}'" class="btn {% if statsComparaisonDispo %}btn-primary{% else %}btn-secondary{% endif %}">Consulter ce type de statistiques</button>
                </div>
            </div>
        </div>
{% else %}
        <div class="info">
            <h1 class="h4 mb-0 text-gray-800">Vous ne pouvez consulter aucun type de statistiques pour le moment.</h1>
            <h1 class="h4 mb-0 text-gray-800">Créez des groupes, des groupes d'étudiants ayant un statut particulier et des évaluations pour consulter les statistiques.</h1>
        </div>
{% endif %}
    <!-- Modale dans laquelle les photos agrandies sont affichées -->
    <div id="modale-images" class="modaleImage">
        <span class="close-modaleImage">&times;</span>
        <img class="modaleImage-content" id="nouvelleImageAffichee">
    </div>
{% endblock %}

{% block javascripts %}

    <script>
        //Event listener pour agrandir l'image lorsqu'on clique dessus
        //La fonction va afficher dans la fenêtre l'image, non réduite à la taille de la carte
        $(".imageAAgrandir").on('click', function () {
            $("#modale-images").css("display", "block");
            $("#nouvelleImageAffichee").attr("src", this.src);
        })
        //Quand on clique sur la croix on ferme
        $(".close-modaleImage").on('click', function () {
            $("#modale-images").css("display", "none");
        })
    </script>


{% endblock %}
